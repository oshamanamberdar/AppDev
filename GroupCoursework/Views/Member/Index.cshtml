
@model IEnumerable<Member>

@{
    ViewData["Title"] = "List of Member";
}

<h1>Add New</h1>
<hr/>
<div>
    <a asp-controller="Member" asp-action="Create" class="btn btn-success">
        <i class="bi bi-plus-circle"></i> Add New
    </a>
    <a asp-controller="Home" asp-action="SearchMemberWithNoCurrentLoans" class="btn btn-success">
            <i class="bi bi-filter"></i> Get Members with raised Loan
    </a>
    
</div>

<br/>

<table class="table table-hover  table-bordered  table-responsive-md text-center sb-small">
    <thead>
    <tr>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Date of Birth</th>
        <th>Address</th>
        <th>Edit</th>
        <th>Delete</th>
    </tr>
            
    </thead> 
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>@Html.DisplayFor(modelItem=> item.MemberFirstName) </td>
            <td>@Html.DisplayFor(modelItem=> item.MemberLastName) </td>
            <td>@Html.DisplayFor(modelItem=> item.MemberDob) </td>
            <td>@Html.DisplayFor(modelItem=> item.MemberAddress) </td>
            <td class="align-middle">
                <a class="btn btn-outline-primary" asp-action="Edit" asp-route-id="@item.Id"><i class="bi bi-pencil-square"></i>Edit </a> 
            </td>
            <td>
                <a class="btn btn-danger text-white" asp-action="Delete" asp-route-id="@item.Id"><i class="bi bi-trash"></i>Delete </a>
            </td>
            
        </tr>
    }

    </tbody>
</table>

